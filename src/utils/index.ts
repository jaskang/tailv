import { any, array, bool, func, number, integer, object, string, symbol } from 'vue-types'

export const PropTypes = {
  any,
  array,
  bool,
  func,
  number,
  integer,
  object,
  string,
  symbol,
}
