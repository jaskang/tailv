import { colors } from '@/core/colors'
import { classed } from '@tw-classed/core'

export type CheckBoxCssVars = {
  '--t-checkbox-text-color': string
  '--t-checkbox-ring-color': string
}

export const getCssVars = (vars: CheckBoxCssVars) => {
  return vars
}
