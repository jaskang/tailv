import { Button } from '@zonda/react'
import { DocSidebar } from '../core/components/DocSidebar'

# Button

<div className="flex items-center space-x-2">
  <Button variant="solid" color="primary">
    Button 
  </Button>

  <Button variant="solid" loading>
    loading
  </Button>
</div>
