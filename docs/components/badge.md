# Badge

## variant

### solid 实心

```vue preview
<template>
  <div class="flex flex-wrap gap-2">
    <TBadge>Badge</TBadge>
    <TBadge color="primary">Badge</TBadge>
    <TBadge color="success">Badge</TBadge>
    <TBadge color="warning">Badge</TBadge>
    <TBadge color="error">Badge</TBadge>
    <TBadge color="orange">Badge</TBadge>
    <TBadge color="teal">Badge</TBadge>
    <TBadge color="sky">Badge</TBadge>
    <TBadge color="pink">Badge</TBadge>
  </div>
</template>
```

### outline 带边框

```vue preview
<template>
  <div class="flex flex-wrap gap-2">
    <TBadge variant="outline">Badge</TBadge>
    <TBadge variant="outline" color="primary">Badge</TBadge>
    <TBadge variant="outline" color="success">Badge</TBadge>
    <TBadge variant="outline" color="warning">Badge</TBadge>
    <TBadge variant="outline" color="error">Badge</TBadge>
    <TBadge variant="outline" color="orange">Badge</TBadge>
    <TBadge variant="outline" color="teal">Badge</TBadge>
    <TBadge variant="outline" color="sky">Badge</TBadge>
    <TBadge variant="outline" color="pink">Badge</TBadge>
  </div>
</template>
```

### pill 圆角

```vue preview
<template>
  <div class="flex flex-wrap gap-2">
    <TBadge variant="solid" pill>Badge</TBadge>
    <TBadge variant="solid" pill color="primary">Badge</TBadge>
    <TBadge variant="solid" pill color="success">Badge</TBadge>
    <TBadge variant="solid" pill color="warning">Badge</TBadge>
    <TBadge variant="solid" pill color="error">Badge</TBadge>
    <TBadge variant="solid" pill color="orange">Badge</TBadge>
    <TBadge variant="solid" pill color="teal">Badge</TBadge>
    <TBadge variant="solid" pill color="sky">Badge</TBadge>
    <TBadge variant="solid" pill color="pink">Badge</TBadge>
  </div>
  <br />
  <div class="flex flex-wrap gap-2">
    <TBadge variant="outline" pill>Badge</TBadge>
    <TBadge variant="outline" pill color="primary">Badge</TBadge>
    <TBadge variant="outline" pill color="success">Badge</TBadge>
    <TBadge variant="outline" pill color="warning">Badge</TBadge>
    <TBadge variant="outline" pill color="error">Badge</TBadge>
    <TBadge variant="outline" pill color="orange">Badge</TBadge>
    <TBadge variant="outline" pill color="teal">Badge</TBadge>
    <TBadge variant="outline" pill color="sky">Badge</TBadge>
    <TBadge variant="outline" pill color="pink">Badge</TBadge>
  </div>
</template>
```

### dot 圆点

```vue preview
<template>
  <div class="flex flex-wrap gap-2">
    <TBadge variant="solid" dot>Badge</TBadge>
    <TBadge variant="solid" dot color="primary">Badge</TBadge>
    <TBadge variant="solid" dot color="success">Badge</TBadge>
    <TBadge variant="solid" dot color="warning">Badge</TBadge>
    <TBadge variant="solid" dot color="error">Badge</TBadge>
    <TBadge variant="solid" dot color="orange">Badge</TBadge>
    <TBadge variant="solid" dot color="teal">Badge</TBadge>
    <TBadge variant="solid" dot color="sky">Badge</TBadge>
    <TBadge variant="solid" dot color="pink">Badge</TBadge>
  </div>
  <br />
  <div class="flex flex-wrap gap-2">
    <TBadge variant="outline" dot>Badge</TBadge>
    <TBadge variant="outline" dot color="primary">Badge</TBadge>
    <TBadge variant="outline" dot color="success">Badge</TBadge>
    <TBadge variant="outline" dot color="warning">Badge</TBadge>
    <TBadge variant="outline" dot color="error">Badge</TBadge>
    <TBadge variant="outline" dot color="orange">Badge</TBadge>
    <TBadge variant="outline" dot color="teal">Badge</TBadge>
    <TBadge variant="outline" dot color="sky">Badge</TBadge>
    <TBadge variant="outline" dot color="pink">Badge</TBadge>
  </div>
</template>
```
